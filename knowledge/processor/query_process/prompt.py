"""查询流程提示词模板"""

ITEM_NAME_EXTRACT_TEMPLATE = """
历史会话：
{history_text}

当前用户问题：{query}

请根据历史会话和当前问题，提取用户正在询问的产品名称或资料名称（item_names）。
1. 如果用户明确提到了产品名称、型号、文档标题或资料名称，请提取出来。可能有一个或多个，但不能重复。
2. 如果用户使用了代词（如"这个"、"它"），请结合历史会话指代消解，确定产品或资料名称。
3. 如果无法确定产品或资料名称，item_names 返回空列表，不要强行猜测。
4. 请重新改写用户的问题（rewritten_query），使其成为独立完整问题。

请直接返回JSON格式结果，格式如下：
{{
    "item_names": ["产品A", "资料B"],
    "rewritten_query": "关于产品A和资料B，..."
}}"""

HYDE_PROMPT_TEMPLATE = """请基于以下用户查询生成一个简洁的回答范文。
用户查询: {query}
要求：
1. 回答要简洁明了，包含核心信息即可
2. 假设你是该领域的专家，提供专业的解释
3. 不要使用"假设"、"可能"等不确定的词汇
4. 保持回答与查询主题高度相关
5. 使用中文回答且不超过300字"""

ENTITY_EXTRACT_SYSTEM_PROMPT = """你是一个知识图谱问答系统的"实体识别"模块。
请从用户问题中抽取用于查询图数据库(Neo4j)的实体名称
（优先抽取设备/部件/操作/工具/条件/警告等名词短语）。

要求：
1) 输出必须是严格 JSON
2) 只输出一个字段：entities
3) entities 为字符串数组，每个元素是实体 name
4) 只抽取与问题相关、且可能在图中作为节点 name 存在的实体；不要输出句子

输出示例：
{"entities":["电池安装","螺丝刀","表笔"]}"""

ANSWER_PROMPT = """你是一个专业的智能助手，请根据以下参考内容回答用户的问题。

【参考内容】
{context}

【历史对话】
{history}

【相关商品】
{item_names}

【图谱关系描述】
{graph_relation_description}

【用户问题】
{question}

要求：
1. 优先基于【参考内容】和【图谱关系描述】作答，不要编造不存在的事实。
2. 如果参考内容不足以回答问题，请如实说明“没有在已上传资料中查询到相关内容”。
3. 回答要清晰有条理，适当使用分点、步骤等格式。
4. 如果问题涉及操作步骤，请按顺序列出。

请回答："""
